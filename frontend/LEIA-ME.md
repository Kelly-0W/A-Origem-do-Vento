# frontend/

Vazia por enquanto, de proposito. Conforme combinado, o Vercel/frontend so
entra quando a criacao de personagem estiver funcionando de ponta a ponta
via terminal (motor de regras + Firestore populado). Quando chegar a hora:

- HTML/CSS/JS puro aqui dentro.
- Deploy no Vercel apontando pra esta pasta.
- PWA (manifest.json + service worker) entra nessa etapa tambem.
