// Carregado antes de cada arquivo de teste (ver `test.setupFiles` em
// vite.config.js) -- só liga os matchers extras do jest-dom
// (toBeInTheDocument, toHaveAttribute etc.) no `expect` do Vitest.
import '@testing-library/jest-dom'
